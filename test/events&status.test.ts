import test from "ava";
import * as delay from "delay";
import * as sinon from "sinon";
import { Spider } from "../src/index";
import { IPlan } from "../src/interfaces";

test("test for status", async (t) => {
  t.is(1, 1);
});
